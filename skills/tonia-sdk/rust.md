# Rust — `tonia-sdk`

The Rust crate is **not shipped**. Do not add `tonia-sdk` to `Cargo.toml`.
See [`compatibility.json`](compatibility.json) (`status: planned`).

Use [TypeScript](typescript.md) or [Python](python.md) until the crate is
published. The same [portal key](setup.md) and [coding tools](tools.md)
steps apply.
